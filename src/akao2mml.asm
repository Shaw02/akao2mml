;|==============================================================|
;|	ＦＩＮＡＬ　ＦＡＮＴＡＳＹ　VIII			|
;|		逆ＭＭＬコンパイラ				|
;|								|
;|				Programmed By			|
;|					Sound Tester:774	|
;|								|
;===============================================================|


.186
.model	tiny,c

.code
;=======================================================================|
;				Start Up				|
;=======================================================================|
	.startup
ff8mml_:	
	MOV	AX,OFFSET CEND + BSTACK	;
	MOV	SP,AX			;

	MOV	AX,CS			;
	MOV	DS,AX			;
	MOV	ES,AX			;セグメントがえへへへ。
	MOV	SS,AX			;

	call	_main
COMEND:
	.exit

;************************************************************************
;*		定義部							*
;************************************************************************
BSTACK		EQU	0400H		;スタックバッファ
;
;	逆ＭＭＬ変換情報
;
;************************************************************************
;*		ルーチン							*
;************************************************************************
include	a2m_main.asm
include	a2m_sub.asm
include	a2m_uc.asm
include	a2m_def.asm

;************************************************************************
;*		終わり							*
;************************************************************************
CEND:
	END
